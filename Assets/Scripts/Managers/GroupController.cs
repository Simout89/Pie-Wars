using UnityEngine;


/// <summary>
/// контролирует группы юнитов.
/// Переключается между группами юнитов
/// Создает группы
/// При нажатие ctrl+z переключится на предыдущих выделенных юнитов
/// </summary>


public class GroupController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
